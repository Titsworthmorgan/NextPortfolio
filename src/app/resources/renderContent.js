import { person, social, home, about, blog, work, gallery } from './content';

const renderContent = () => {
    return {
        person,
        social,
        home,
        about,
        blog,
        work,
        gallery
    }
};

export { renderContent };
